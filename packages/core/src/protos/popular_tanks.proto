syntax = "proto2";

package blitzkit;

message PopularTanks {
  repeated PopularTank tanks = 1;
}

message PopularTank {
  required uint32 id = 1;
  required uint32 views = 2; // views in the last 7 days
}