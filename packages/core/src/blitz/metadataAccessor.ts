import { Metadata } from '@protos/blitz_metadata';

export class MetadataAccessor {
  constructor() {}

  loadMetadata(metadata: Metadata) {
    for (const catalogItem of metadata.catalog_items) {
    }
  }
}
